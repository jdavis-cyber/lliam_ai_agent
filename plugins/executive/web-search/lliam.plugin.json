{
  "id": "executive.web-search",
  "name": "Web Search",
  "version": "1.0.0",
  "description": "Real-time web search via Brave Search API. Returns ranked results with titles, URLs, and snippets. Use when Claude's training data may be stale or when Jerome needs live information.",
  "kind": "general",
  "configSchema": {
    "type": "object",
    "properties": {
      "braveApiKey": {
        "type": "string",
        "description": "Brave Search API key (reads from BRAVE_API_KEY env var if not set)"
      },
      "maxResults": {
        "type": "number",
        "description": "Maximum results to return (default: 5)"
      }
    }
  }
}
